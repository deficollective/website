@AGENTS.md

# Git

- Never push changes — always ask before pushing.
- Never push with unconfigured user name and email.
- Never append "Co-Authored-By: Claude" to commit messages.

# Workflow

- Always clean up after making changes (remove unused code, imports, files, dependencies).
